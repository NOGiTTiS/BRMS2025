<IfModule mod_rewrite.c>
  RewriteEngine On
  # เพิ่มบรรทัดนี้เข้าไป
  RewriteBase /brms/public/

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]
</IfModule>